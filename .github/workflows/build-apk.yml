name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.47.2'
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: |
          cd mena_recruitment
          touch .env
          flutter pub get

      - name: Build Android Release APK
        run: |
          cd mena_recruitment
          flutter build apk --release --dart-define=API_BASE_URL=https://mena-mobile-recruitment-platform.onrender.com/api/v1 --dart-define=WS_BASE_URL=wss://mena-mobile-recruitment-platform.onrender.com/api/v1/ws

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mena-recruitment-release-apk
          path: mena_recruitment/build/app/outputs/flutter-apk/app-release.apk

      - name: Publish to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: "Global Jobs By Suhana — Latest Build"
          body: "Official Android APK release for Global Jobs By Suhana."
          files: mena_recruitment/build/app/outputs/flutter-apk/app-release.apk
